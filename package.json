{
  "publisher": "LiuQi",
  "name": "stata-preview",
  "displayName": "%displayName%",
  "version": "0.1.3",
  "description": "%description%",
  "license": "MIT",
  "homepage": "https://github.com/liuqi6908/stata-preview",
  "repository": {
    "type": "git",
    "url": "https://github.com/liuqi6908/stata-preview.git"
  },
  "bugs": {
    "url": "https://github.com/liuqi6908/stata-preview/issues"
  },
  "keywords": [
    "stata",
    "dta",
    "data-preview",
    "statistics"
  ],
  "categories": [
    "Visualization",
    "Data Science"
  ],
  "main": "./dist/extension.js",
  "icon": "media/logo.png",
  "l10n": "./dist/l10n",
  "engines": {
    "vscode": "^1.120.0"
  },
  "activationEvents": [],
  "contributes": {
    "customEditors": [
      {
        "viewType": "stataPreview.dta",
        "displayName": "%editorContributeName%",
        "selector": [
          {
            "filenamePattern": "*.dta"
          }
        ],
        "priority": "default"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "tsc --noEmit && pnpm test && node esbuild.js --production",
    "compile": "node esbuild.js",
    "watch": "node esbuild.js --watch",
    "lint": "eslint . --fix",
    "test": "tsc --noEmit -p tsconfig.test.json && node scripts/run-tests.mjs",
    "l10n": "npx @vscode/l10n-dev export --outDir ./l10n ./src",
    "build": "vsce package"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^9.0.0",
    "@types/node": "24.x",
    "@types/vscode": "^1.120.0",
    "@vscode/l10n-dev": "^0.0.35",
    "esbuild": "^0.28.0",
    "eslint": "^10.4.0",
    "typescript": "^5.9.3"
  }
}
